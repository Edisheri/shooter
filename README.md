# Базовый шутер на C++ (SFML)

Простой 2D-шутер с видом сверху, написанный на C++ с использованием библиотеки SFML.

## 🎮 Особенности игры
- Управление персонажем с помощью клавиатуры (WASD)
- Прицеливание и стрельба мышью
- Бесконечные волны врагов
- Подсчет очков
- Простая физика столкновений

## 📋 Требования

### Для Linux (Ubuntu/Debian)
```bash
sudo apt-get update
sudo apt-get install libsfml-dev g++ make
```

### Для Windows
1. Установите [MinGW](https://www.mingw-w64.org/) или MSVC
2. Скачайте и установите [SFML](https://www.sfml-dev.org/download.php)
3. Добавьте путь к библиотекам SFML в переменную окружения или укажите при компиляции

### Для macOS
```bash
brew install sfml
```

## 🚀 Установка и запуск

### Вариант 1: Быстрый запуск (Linux/Mac)

Если у вас уже установлены `g++` и `SFML`, просто выполните:

```bash
# Компиляция
g++ -o shooter shooter.cpp -lsfml-graphics -lsfml-window -lsfml-system

# Запуск
./shooter
```

### Вариант 2: Использование Makefile

```bash
# Компиляция
make

# Запуск
./shooter

# Очистка (удалить исполняемый файл)
make clean
```

### Вариант 3: Windows (Command Prompt)

Предполагается, что SFML установлен в `C:\SFML`:

```cmd
g++ -o shooter.exe shooter.cpp -IC:\SFML\include -LC:\SFML\lib -lsfml-graphics -lsfml-window -lsfml-system
shooter.exe
```

*Примечание: Убедитесь, что DLL-файлы SFML находятся в той же папке, что и `.exe`, или в системном PATH.*

## 🎮 Управление

| Действие | Клавиша/Кнопка |
|----------|----------------|
| Движение вперед | `W` или `↑` |
| Движение назад | `S` или `↓` |
| Движение влево | `A` или `←` |
| Движение вправо | `D` или `→` |
| Стрельба | Левая кнопка мыши (ЛКМ) |
| Прицеливание | Движение мыши |
| Выход из игры | `Esc` или закрытие окна |

## 🎯 Цель игры
- Уничтожайте красных врагов, появляющихся со всех сторон
- Избегайте столкновений с врагами
- Набирайте максимальное количество очков
- Игра усложняется со временем (враги появляются чаще)

## 🛠️ Структура проекта

```
.
├── shooter.cpp       # Исходный код игры
├── Makefile          # Скрипт для сборки
├── README.md         # Этот файл
└── shooter           # Исполняемый файл (после сборки)
```

## 🔧 Решение проблем

### Ошибка: "undefined reference to sf::..."
Убедитесь, что библиотеки SFML правильно подключены при компиляции. Проверьте флаги `-lsfml-graphics -lsfml-window -lsfml-system`.

### Ошибка: "SFML/Graphics.hpp not found"
Установите пакет разработки SFML (`libsfml-dev` на Linux) или проверьте пути включения (`-I`) на Windows.

### Игра запускается, но окно сразу закрывается
Проверьте, установлены ли все зависимости и драйверы графики. Попробуйте запустить игру из терминала, чтобы увидеть сообщения об ошибках.

## 📝 Лицензия
Этот проект создан в образовательных целях. Вы можете свободно использовать и модифицировать код.

## 🤝 Вклад в развитие
Чувствуйте себя свободным в улучшении игры! Вы можете добавить:
- Новые типы врагов
- Разное оружие
- Звуковые эффекты
- Текстуры и спрайты
- Меню и экраны паузы

Приятной игры! 🎮
